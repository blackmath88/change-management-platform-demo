# Optional authenticated synchronization. Never commit production credentials.
# The anon key is public by design; Row-Level Security remains the access boundary.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
